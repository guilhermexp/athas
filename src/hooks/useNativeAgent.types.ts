/**
 * Type definitions for useNativeAgent hook
 * Re-exported for convenience
 */

export type {
  Agent,
  // Agent types
  AgentCallbacks,
  AgentContext,
  AgentType,
  MessageContent,
  Thread,
  ThreadMessage,
  ThreadMetadata,
  ToolApproval,
  ToolCall,
} from "@/components/agent-panel/types";
export type {
  // Hook types
  UseNativeAgentOptions,
  UseNativeAgentReturn,
} from "./useNativeAgent";
